name: Build and Push to GHCR

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      # 1. 自动生成版本号
      - name: Set Image Tag
        id: vars
        run: |
          # 生成格式：年月日-短哈希 (例如 20260201-7b3a4e2)
          echo "tag=$(date +'%Y%m%d')-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          # 动态标签：同时推送到最新版和具体的版本号
          # 包名必须与本仓库同名，GITHUB_TOKEN 才有写权限；
          # 旧仓库 zvps-super 的 ghcr.io/<owner>/zvps 独立存在，不要复用
          tags: |
            ghcr.io/${{ github.repository_owner }}/docker-opencode:latest
            ghcr.io/${{ github.repository_owner }}/docker-opencode:${{ steps.vars.outputs.tag }}

      # 2. 在控制台打印出来，方便复制版本号
      - name: Image info
        run: |
          echo "镜像构建完成！"
          echo "版本标签为: ${{ steps.vars.outputs.tag }}"
